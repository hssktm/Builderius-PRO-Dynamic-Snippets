# name: Direct Logout with Redirect
# desc: Generates a secure logout URL that skips confirmation and redirects to a custom page
{
  target_redirect: expression_result(expression: "'/pagina-especial'") @private
  
  logout_data: php_function_output(
    function: "wp_logout_url"
    arguments: ["{{target_redirect}}"]
  )
}
